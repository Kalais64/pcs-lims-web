# Public client env only (RLS applies). Never put SERVICE_ROLE or DATABASE_URL here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Explicit local/demo fallback. If unset/false and Supabase public env is present, the app uses live data.
NEXT_PUBLIC_USE_FIXTURES=false
